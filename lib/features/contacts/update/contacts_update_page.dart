import 'package:flutter/material.dart';

class ContactsUpdatePage extends StatelessWidget {
  const ContactsUpdatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Update'),
      ),
      body: Container(),
    );
  }
}
